---
kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: run tests
    image: golang:1.16
    commands:
      - make test
    environment:
      SHELL: /bin/bash
